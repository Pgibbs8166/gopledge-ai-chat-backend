# GoPledge AI Chat Backend

## Setup Instructions

1. Run `npm install`
2. Set the environment variable `OPENAI_API_KEY` with your OpenAI API key.
3. Run `node server.js` to start the server.
4. POST to `/api/chat` with a JSON body like `{ "message": "What is GoPledge?" }`
